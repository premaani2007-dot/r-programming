temp <- as.numeric(readline("Enter temperature: "))
unit <- toupper(readline("Enter unit (C/F): "))

if (unit == "C") {
  f <- (temp * 9/5) + 32
  cat("Fahrenheit =", f)
} else if (unit == "F") {
  c <- (temp - 32) * 5/9
  cat("Celsius =", round(c, 2))
} else {
  cat("Invalid unit")
}
